name: Docker Compose CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🛠️ Build Docker images
        run: |
          docker compose -f docker-compose.prod.yml build

      - name: 🐳 Tag and Push Backend image
        run: |
          docker tag todo101-todo-backend:latest ${{ secrets.DOCKER_USERNAME }}/todo101-backend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/todo101-backend:latest

      - name: 🐳 Tag and Push Frontend image
        run: |
          docker tag todo101-todo-frontend:latest ${{ secrets.DOCKER_USERNAME }}/todo101-frontend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/todo101-frontend:latest

